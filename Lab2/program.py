"""Лабораторная работа №2. Расчёт яркости на сфере от точечных источников света.

Точка входа приложения: запускает графический интерфейс, в котором задаются
параметры сцены, источники света и свойства поверхности (модель Блинн-Фонга),
рассчитывается и визуализируется распределение яркости на сфере.

Запуск:
    python program.py
"""

from sphere_luminance.ui import ApplicationWindow


def main() -> None:
    """Создаёт главное окно приложения и запускает цикл обработки событий."""
    ApplicationWindow().mainloop()


if __name__ == "__main__":
    main()
